import React from 'react';
import Spinner from '.';

export default {
  title: 'components/Spinner',
  component: Spinner,
  argTypes: {},
};

export function Default(args) {
  return <Spinner {...args} />;
}
