import React from 'react';
import PageTemplate from '../../feature/pageTemplate/PageTemplate';
import BaseCardContainer from '../../components/BaseCardContainer';

const TTaBongPage = () => {
  return (
    <PageTemplate>
      {/* rem으로 넣음 */}
      <BaseCardContainer height={37} />
    </PageTemplate>
  );
};

export default TTaBongPage;
